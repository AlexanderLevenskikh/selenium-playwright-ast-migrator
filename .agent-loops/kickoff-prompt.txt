PRIMARY LOOP PROMPT for Migrator Autopilot Loop.
Use this file as the single source prompt for new autopilot runs; secondary prompts must defer to it and must not redefine conflicting loop rules.

Read all files in .agent-loops/.
Also read AGENTS.md, docs/autopilot-loop.md, .agent-loops/13-loop-contract.md, .agent-loops/11-strict-ticket-boundaries.md, .agent-loops/12-pom-helper-recovery-policy.md, and .agent-loops/15-stop-policy-checklist.md.

Start Migrator Autopilot Loop.

Mode: migration-artifact

You are allowed and expected to make engineering decisions yourself.
Do not ask me to choose between implementation options.
Do not ask “continue?” after partial progress or whenever status is CONTINUE_AUTONOMOUSLY.
Do not stop after partial progress.
Continue until the selected migration block is fixed and verified, or until the stop policy requires a real stop.

Migration scope:
- Source Selenium project: <SOURCE_SELENIUM_PROJECT_PATH>
- Target/generated Playwright project: <TARGET_PROJECT_OR_OUTPUT_PATH>
- Migrator config/profile: <CONFIG_OR_PROFILE_PATH>
- Compiled migrator tool, if compiled-tool-only mode: <COMPILED_TOOL_PATH_OR_EMPTY>
- Existing Playwright POM examples: <TARGET_POM_EXAMPLES_PATH_OR_EMPTY>
- Verify/orchestrate output directory: <OUTPUT_DIR>
- Latest migration board: <PATH_OR_EMPTY>
- Latest project verify report: <PATH_OR_EMPTY>
- Expected verify/orchestrate command: <COMMAND_OR_EMPTY>
- Expected project verify command: <COMMAND_OR_EMPTY>

Allowed input paths:
- <ALLOWED_INPUT_PATH_1>
- <ALLOWED_INPUT_PATH_2>

Allowed write paths:
- <ALLOWED_WRITE_PATH_1>

Forbidden paths:
- parent directories outside allowed input/write roots
- repository source folders unless explicitly listed as allowed write paths
- migrator repository source code unless Mode is migrator-code

Loop contract:
- Goal: <ONE_CONCRETE_BATCH_GOAL>
- Max iterations: <N>
- Check command: <COMMAND>
- Exit condition: <OBJECTIVE_REPORT_OR_COMMAND_CONDITION>
- State file: .agent-state/migrator-progress.md

Strict path rules:
- Do not search parent directories.
- Do not edit source files unless the repository source tree is listed in allowed write paths.
- If this task provides DLL/artifact folders, treat them as source of truth and do not locate matching source code.
- Before any write, verify the target path is inside an allowed write root.

State/resume:
- Create/update .agent-state/migrator-progress.md after every meaningful batch.
- Create/update .agent-state/current-migration-batch.md while a batch is in progress.
- Save important verify output to .agent-state/last-verify-output.md when useful.
- Follow .agent-loops/10-state-and-resume.md for long-running loops.

Command/path rule:
- If compiled migrator tool is provided, run that tool directly and do not search for `Migrator.Cli`, `.sln`, `.csproj`, or migrator source code.
- If migrator engine work appears necessary in migration-artifact mode, record a source-change ticket candidate instead of editing repository code.
- Repository source edits are forbidden unless Mode is exactly migrator-code and the repository source tree is listed under allowed write paths.

POM/helper recovery rules:
- Run or inspect `index-pom` before large PageObject/config mapping work.
- Run or inspect `helper-inventory` before mapping/suppressing project/POM helpers.
- Missing target Playwright POM coverage is not automatically `TICKET_NEEDED`.
- If Selenium POM has real selector evidence (`ByTId`, `CreateControlByTid`, `data-tid`, CSS, XPath, resolved constants), use it.
- Do not invent selectors. PageObject class/property names are not selectors.
- Prefer existing target POM → generated POM in migration output → raw locator from proven selector → TODO.
- Do not modify production target POMs unless explicitly allowed.

Current task:
<PASTE CURRENT BLOCK / ERROR / LOG / TODO CATEGORY HERE>

Important continuation rule:
- A green build/project verify is a checkpoint, not final migration completion, unless the task is explicitly compile-only.
- If a batch is READY_FOR_ACCEPTANCE but the migration board still has actionable work, overall status is CONTINUE_AUTONOMOUSLY.
- Migration-quality trade-offs are not a stop reason by themselves. Choose the smallest safe reversible batch.

Before any stop, fill/check .agent-loops/15-stop-policy-checklist.md. If no hard stop condition is satisfied and status is CONTINUE_AUTONOMOUSLY, continue without asking the user.

Use only allowed repository code, existing tests, snapshots, docs, CLI reports, migration board, source Selenium tests, target project conventions, generated output, and command output as the source of truth.
