SECONDARY RESUME PROMPT. This prompt resumes the primary loop from `.agent-loops/kickoff-prompt.txt`; it must not redefine conflicting rules.

Read all files in .agent-loops/.
Read .agent-loops/13-loop-contract.md.
Read .agent-loops/11-strict-ticket-boundaries.md.
Read .agent-loops/15-stop-policy-checklist.md.
Read .agent-state/migrator-progress.md if it exists.

Resume Migrator Autopilot Loop.

Mode: migration-artifact

Allowed input paths:
- <ALLOWED_INPUT_PATH_1>
- <ALLOWED_INPUT_PATH_2>

Allowed write paths:
- <ALLOWED_WRITE_PATH_1>

Forbidden paths:
- parent directories outside allowed input/write roots
- repository source folders unless explicitly listed as allowed write paths

Do not search outside allowed paths. Do not edit source files unless explicitly allowed. Do not ask “continue?”.
Do not ask me whether to continue if status is CONTINUE_AUTONOMOUSLY.
Do not edit migrator repository source code unless Mode is migrator-code and the repository is an allowed write path.

Do not assume the previous agent's claims are true.
First reconcile saved state with actual repository state.

Resume steps:
1. Inspect git status and git diff.
2. Inspect the latest migration board/report.
3. Inspect the latest project verify report.
4. Read the last recorded batch and command results.
5. If POM/helper categories are present, inspect latest `index-pom` and `helper-inventory` outputs or run them before changing mappings/suppressions.
6. Run the strongest affordable verification commands for the current scope.
7. Classify current state:
   - last batch kept;
   - last batch reverted;
   - last batch unsafe;
   - last batch unknown;
   - environment blocked.
8. Do not start a new batch until the current batch is classified.
9. Continue from the last safe checkpoint.

If the previous run drifted into migrator source-code repair while Mode is
migration-artifact, stop that line of work, record a source-change ticket
candidate, and resume the allowed migration-artifact batch.

If the last batch was compile-fix and project verify is green:
- treat that batch as READY_FOR_ACCEPTANCE;
- treat overall migration as CONTINUE_AUTONOMOUSLY if the board still has actionable categories;
- continue according to .agent-loops/08-continuation-rule.md.

Before stopping or handing off, apply .agent-loops/15-stop-policy-checklist.md.
Do not ask me whether to continue if status is CONTINUE_AUTONOMOUSLY or RESUME_READY.
