Resume the latest standard migration run and open a fresh five-cycle invocation budget. Preserve current evidence and exhausted candidate fingerprints, but reset the invocation cycle counter and no-progress streak. Rank non-exhausted source-backed candidates, execute one bounded change per cycle, rerun the complete source after each change, and compare evidence. Continue automatically after progress. After the first no-progress cycle, exhaust that candidate and try a different independent candidate. Stop only after two consecutive distinct no-progress cycles, a real blocker, a human-only decision, or budget exhaustion. A project-verification harness defect does not block independent measurable config/POM/helper improvements. Never manufacture validation artifacts or hand routine safe work back as an opt-in question. In `continuous` mode, reaching the five-cycle checkpoint automatically opens the next five-cycle batch; do not require the user to invoke `continue` again. Before writing `No further automated migration work remains`, list every remaining root-cause cluster with its count, representative evidence, and a concrete stop reason; use that sentence only when no safe agent-executable candidate remains.
