SECONDARY GUARDED RESUME PROMPT.
Resume an interrupted guarded migration run without using legacy root prompt packs.

Read first:
- `{{WORKSPACE}}/AGENT_CONTRACT.md`
- `{{WORKSPACE}}/agent-state.md`
- `{{WORKSPACE}}/current-ticket.md`
- `{{WORKSPACE}}/state/run-ledger.md`
- `{{WORKSPACE}}/state/handoff.md`
- `{{WORKSPACE}}/state/stop-policy-checklist.md`
- `{{WORKSPACE}}/state/final-gate.md`
- `{{WORKSPACE}}/state/continuation-contract.md`, if present
- `{{WORKSPACE}}/state/continuation-decision.json`, if present

Mode: migration-artifact

Resume protocol:
1. Identify latest run id from state files and latest `{{WORKSPACE}}/runs/run-*` artifacts.
2. Reconstruct the last blocker, last command, and last verified evidence.
3. Run `{{WORKSPACE}}/scripts/check-scope.ps1` before any edit.
4. Continue only with an allowed write under `{{WORKSPACE}}/**`.
5. If the next useful action would write outside `{{WORKSPACE}}/**`, stop with `BLOCKED_BY_FORBIDDEN_WRITE` and write a proposal under `{{WORKSPACE}}/proposals/**`.

Rules:
- If status is `CONTINUE_AUTONOMOUSLY`, continue without asking the user whether to continue.
- Do not edit migrator source code in migration-artifact mode.
- Do not add suppressions, weaken assertions, empty tests, add dummy known identifiers, or edit target/POM project to reduce TODO.
- Before stopping or handing off, update `{{WORKSPACE}}/state/stop-policy-checklist.md`.
- After a non-final final-gate run, read `{{WORKSPACE}}/state/continuation-decision.json`. If it says `CONTINUE_REQUIRED`, execute exactly one next bounded action before any user-facing handoff. A response that only repeats `NOT FINAL` / `NOT RUNTIME READY` while continuation is required is a protocol violation.
- Do not claim FINAL unless `{{WORKSPACE}}/scripts/check-final-gate.ps1 -RequireOpenCodeExport -RequireExplainTodo -RequireVerificationArtifacts` passes; otherwise report `NOT FINAL - INVESTIGATION RESULT ONLY`.
