Run one bounded improvement cycle: inspect current evidence and accepted baseline, call `selenium-pw-migrator remediation guard` against the current source/config, and open the transaction with `update-autonomy-state -Action StartCycle -GuardPath ...` before editing. Make one bounded change, rerun the complete configured source into a new immutable run, then call `selenium-pw-migrator remediation evaluate` for the before/after runs and record only the Core evaluation. On any `REJECT_*`, restore the complete bounded change; another cycle may start only after Core returns `ROLLBACK_CONFIRMED`. On `REJECT_CYCLE`, confirm rollback before handoff and stop. Never author PROGRESS/NO_PROGRESS/BLOCKED yourself, partition the source, bypass failed verification, or confuse syntax validity with a passing project build.
