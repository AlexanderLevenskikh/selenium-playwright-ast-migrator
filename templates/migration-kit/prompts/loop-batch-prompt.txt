SECONDARY GUARDED BATCH PROMPT.
This prompt runs one bounded batch under `{{WORKSPACE}}/AGENT_CONTRACT.md`; it must not redefine the contract.

Read first:
- `{{WORKSPACE}}/AGENT_CONTRACT.md`
- `{{WORKSPACE}}/state/final-gate.md`
- `{{WORKSPACE}}/state/continuation-contract.md`, if present
- `{{WORKSPACE}}/state/continuation-decision.json`, if present
- `{{WORKSPACE}}/state/stop-policy-checklist.md`
- latest `{{WORKSPACE}}/agent-state.md`, `{{WORKSPACE}}/current-ticket.md`, and `{{WORKSPACE}}/state/run-ledger.md`

Mode: migration-artifact

Batch objective:
- Pick exactly one evidence-backed blocker/root cause from the latest migration board, explain-todo, config-validate, verify, or project-verify artifacts.
- Apply the smallest allowed change under `{{WORKSPACE}}/**`.
- Do not edit migrator source code, `Web/**`, real POM project, real Playwright project, `*.csproj`, `nuget.config`, or root-level generated files.
- Do not add suppressions or dummy target-known identifiers to reduce TODO.

Required checks after the batch:
1. Run `{{WORKSPACE}}/scripts/check-scope.ps1`.
2. Run config validation / quality dashboard command when available.
3. Run the smallest relevant migrate/verify command.
4. Update workspace state files.
5. Run `{{WORKSPACE}}/scripts/check-final-gate.ps1`; use strict flags only when claiming FINAL.
6. If `{{WORKSPACE}}/state/continuation-decision.json` says `CONTINUE_REQUIRED`, execute one next bounded action before reporting to the user.

Continuation rule:
- If status is `CONTINUE_AUTONOMOUSLY` for a non-final batch, continue without asking the user whether to continue.
- Stop only for a classified blocker, forbidden write requirement, loop/plateau, missing input, or strict final gate result.
- If the same failure appears twice without new evidence, stop with `LOOP_DETECTED` and write root-cause analysis under `{{WORKSPACE}}/runs/<run-id>/`.

FINAL rule:
After a non-final final-gate run, read `{{WORKSPACE}}/state/continuation-decision.json`. If it says `CONTINUE_REQUIRED`, execute exactly one next bounded action before any user-facing handoff. A response that only repeats `NOT FINAL` / `NOT RUNTIME READY` while continuation is required is a protocol violation.
After a successful `FINAL` / PASS checkpoint, stop and report. Start another run only when the user explicitly says `continue` or bounded auto-continuation is recorded for this exact next action.
- Do not claim FINAL unless:
`{{WORKSPACE}}/scripts/check-final-gate.ps1 -RequireOpenCodeExport -RequireExplainTodo -RequireVerificationArtifacts` passes.
Otherwise report `NOT FINAL - INVESTIGATION RESULT ONLY`.
