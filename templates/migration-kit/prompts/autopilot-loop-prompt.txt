You are running a Migrator Agent Harness Kit autopilot batch.

Read these files first, in order:

1. migration/AGENT_CONTRACT.md
2. migration/state/harness-policy.json
3. migration/agent-state.md
4. migration/current-ticket.md
5. migration/state/handoff.md
6. migration/runs/<latest-run-id>/Prompt.md
7. migration/runs/<latest-run-id>/Plan.md
8. migration/runs/<latest-run-id>/Implement.md

Core rule:
When the current status is `CONTINUE_AUTONOMOUSLY`, do not ask the user whether to continue if the next action is allowed by the contract and harness policy. Continue autonomously inside the allowed lane.

Allowed lane:
- read/search repository files;
- write/update migration artifacts under migration/**;
- run listed allowed verification commands;
- run scope checks, `check-harness-policy.ps1`, and final-gate checks;
- fix failures caused by your own allowed migration-artifact edits.

Ask or stop only for:
- writes outside migration/**;
- changes to guard scripts, checksum manifests, OpenCode permissions, or AGENTS.md;
- real product/POM/Playwright project edits in artifact-only mode;
- package install/update or network access;
- git commit/push/reset/clean;
- destructive delete/move operations;
- ambiguous business/product decisions that cannot be resolved from source truth.

Batch loop:
1. Run `check-harness-policy.ps1` if possible.
2. Inspect current run artifacts.
3. Execute one bounded migration improvement.
4. Record evidence under migration/runs/<latest-run-id>/ or migration/reports/.
5. Run scope check.
6. If scope passes, run the relevant migrator/config/verify command.
7. Update Documentation.md, state/handoff.md, current-ticket.md, and run-ledger.md.
8. Run final gate only when claiming FINAL.

Final answer rules:
- Say FINAL only if final gate passed.
- Otherwise say NOT FINAL - INVESTIGATION RESULT ONLY.
- Include exact failed checks and next autonomous step.
