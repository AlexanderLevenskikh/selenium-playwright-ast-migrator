Read AGENT_CONTRACT.md, state/source-scope.json, and the project-local memory summary. Run doctor, inspect memory with `selenium-pw-migrator memory explain --workspace migration`, create an optional representative pilot only when missing, then execute one full `selenium-pw-migrator run` for the configured source. Run real `verify-project` when possible and `selenium-pw-migrator memory doctor --workspace migration` before handoff. Do not create batch plans or synthetic evidence. Report the exact run path, metrics, blockers, and one highest-payoff next action. Run the installed scope, artifact, and final-gate checks for the exact run; missing or failed project verification is a blocker, not a pass.
