# Harness dashboard assets

This directory contains static dashboard assets for `scripts/build-harness-dashboard.ps1`.

- `i18n/en.json` is the canonical English UI dictionary.
- `i18n/ru.json` is the secondary Russian localization.

Dashboard input data stays language-neutral: event/status/action codes are not localized in JSONL or report JSON.
